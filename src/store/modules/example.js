const state = {
    data: 'example',
    count: 1
};
const getters = {};
const actions = {
    
};
const mutations = {
	incrementExample(state) {
      	// mutate state
      	state.count++
    }
};
export default {state, actions, mutations, getters};